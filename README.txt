1. make the changes to the files
2. cd /cygdrive/c/Users/unope/git/gen3-calc
3. node build
4. Commit and push updated files
5. git subtree push --prefix dist origin gh-pages
5a. username = UnopenedClosure
5b. password comes ftom gitkey file